layout(location=0) out float outTau;

in VS_OUT {
    float vAlong;
    float side;
} fs_in;

void main()
{
    float along = mix(0.45, 1.0, 1.0 - fs_in.vAlong);
    float cross = pow(max(0.0, 1.0 - abs(fs_in.side)), 0.8);
    
    float alphaDom = 0.08 * along * cross;

    outTau = alphaDom;
}